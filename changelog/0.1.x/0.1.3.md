---
summary: "Ecosystem metadata alignment — scoped package scripts, author/funding fields, FUNDING.yml, README install badges"
breaking: false
security: false
---

# 0.1.3 — 2026-05-26

## Added

- **`.github/FUNDING.yml`** — GitHub Sponsors and Buy Me a Coffee links
- **README install badges** — Install in Claude Desktop (`.mcpb`), Install in Cursor, Install in VS Code one-click install buttons; Docker and npm badges in the version row

## Changed

- **`package.json` scripts** — migrated from `tsx`/`npm run` to `bun run scripts/*.ts` pattern; `test` now uses `bunx vitest run`; `start`/`start:stdio`/`start:http` use `bun ./dist/index.js`; `bundle` uses `bun run build`
- **`package.json` metadata** — added `author`, `funding` (GitHub Sponsors + Buy Me a Coffee), `packageManager`, `bugs`, and `homepage` fields
- **`manifest.json`** — bumped version to 0.1.3; added server description and author name
- **README version badge** — updated to 0.1.3; Bun badge corrected to v1.3.0; added Docker and npm badges
